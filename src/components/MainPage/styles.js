import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  home: {
    background: '#FA8072',
  },
  item2: {
    background: '#00FF00',
  },
});

export default useStyles;
